# ASP Platform - Environment Configuration Template
# Copy this file to .env and fill in the actual values
#
# For GitHub Codespaces: Add these as repository secrets instead
# https://github.com/evelynmitchell/Process_Software_Agents/settings/secrets/codespaces

# =============================================================================
# REQUIRED: API Keys
# =============================================================================

# Anthropic API Key (required for Claude LLM access)
# Get from: https://console.anthropic.com/settings/keys
# Cost: Pay-per-use, ~$0.08-0.15 per simple task through the pipeline
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Langfuse Observability (required for telemetry and tracing)
# Get from: https://cloud.langfuse.com → Settings → API Keys
# Free tier available
LANGFUSE_PUBLIC_KEY=pk-lf-your-key-here
LANGFUSE_SECRET_KEY=sk-lf-your-key-here
LANGFUSE_HOST=https://cloud.langfuse.com

# =============================================================================
# OPTIONAL: Application Settings
# =============================================================================

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Database path (SQLite, auto-created if doesn't exist)
DATABASE_URL=sqlite:///./data/asp_telemetry.db

# Web UI user ID (for telemetry attribution)
ASP_USER_ID=developer@asp-platform.local
